/// Shared corner radii — buttons, cards, and inputs use [sm] (10px).
abstract final class AppRadii {
  static const sm = 10.0;
  static const md = 10.0;
  static const lg = 12.0;
  static const pill = 100.0;
}
